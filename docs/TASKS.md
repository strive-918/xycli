# Tasks: XYCLI Buildable Skeleton

> Generated by Planner Agent | Stage 3 of software-development workflow

**Total estimated tasks:** 62  
**Estimated effort:** 48.75 hours

---

## Milestone Overview

| Milestone | Goal | Estimated |
|---|---|---:|
| M1 | 单包最小可运行 CLI：agent loop、Anthropic provider、`file_read`/`file_write`/`terminal_exec`、JSON session、工具权限边界；必须跑通 `xycli "list files"` | 7.75 h |
| M2 | 增加 `search_text`、`web_fetch`、`git_status`/`git_diff`，改进 streaming、colors、progress | 4.5 h |
| M3 | 多 provider：OpenAI adapter、provider fallback、retry/circuit-breaker | 4.75 h |
| M4 | SQLite persistence：session store、resume、session list/show | 4.5 h |
| M5 | Plan mode：执行前生成计划，用户批准后再行动 | 3.5 h |
| M6 | Memory：跨 session 记忆用户偏好和项目上下文 | 3.75 h |
| M7 | Computer use：增强 terminal control、browser hooks、screenshot | 4.0 h |
| M8 | Extensibility：MCP client、plugin system、allowlist/denylist config | 5.5 h |
| M9 | Safety：permission engine、approval gate、secret redaction、undo/rollback | 5.75 h |
| M10 | Polish：telemetry、diagnostics、npm packaging、CI/CD | 4.75 h |

---

## M1: Core Skeleton

**Working CLI:** `npm run build && node dist/cli.js "list files"` and `npm link && xycli "list files"`

```
M1-T01 -> M1-T02 -> M1-T03 -> M1-T04 -> M1-T05 -> M1-T06 -> M1-T07 -> M1-T08 -> M1-T09
```

### M1-T01: Root TypeScript CLI Scaffolding

**Objective:** 建立单包 TypeScript CLI 骨架，先不引入 monorepo，确保最快可构建。

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `src/cli.ts`
- Create: `src/version.ts`
- Create: `vitest.config.ts`
- Create: `.gitignore`

**Steps:**
1. 配置 `type: module`、`bin.xycli: dist/cli.js`、`scripts.build/test/dev`。
2. 安装 `typescript`、`commander`、`zod`、`vitest`、`tsx`、`@types/node`。
3. 在 `src/cli.ts` 解析默认 positional prompt，支持 `xycli --version` 和 `xycli --help`。
4. Verification: `npm run build && node dist/cli.js --help`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M1-T02: Core Domain Interfaces

**Objective:** 定义可扩展的 provider、tool、session、agent loop 接口，后续拆包不重写调用边界。

**Files:**
- Create: `src/core/types.ts`
- Create: `src/core/errors.ts`
- Create: `src/tools/types.ts`
- Create: `src/providers/types.ts`
- Create: `src/session/types.ts`

**Steps:**
1. 定义 `ITool`、`ToolExecutionContext`、`ToolResult`、`PermissionLevel`。
2. 定义 `IProvider`、`ProviderRequest`、`ProviderStreamEvent`、`NormalizedToolCall`。
3. 定义 `SessionRecord`、`SessionMessage`、`ToolCallRecord`。
4. 定义 exit code 对应的 `XycliError` 子类。
5. Verification: `npm run build`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M1-T03: JSON File Session Store

**Objective:** 用 JSON 文件保存最小 session，支持 agent loop 审计和 M4 SQLite 迁移。

**Files:**
- Create: `src/session/json-store.ts`
- Create: `src/session/index.ts`
- Create: `src/session/json-store.test.ts`

**Steps:**
1. 默认写入 `.xycli/sessions/json/<session-id>.json`。
2. 记录 user message、assistant message、tool call input/result、status、createdAt、updatedAt。
3. 使用 atomic write 到临时文件再 rename，避免半写入。
4. Verification: `npm test -- src/session/json-store.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M1-T04: Built-In Tool Registry

**Objective:** 注册和执行内置 tools，先实现 schema validation、timeout、structured result。

**Files:**
- Create: `src/tools/registry.ts`
- Create: `src/tools/validation.ts`
- Create: `src/tools/index.ts`
- Create: `src/tools/registry.test.ts`

**Steps:**
1. 实现 `ToolRegistry.register()`、`get()`、`list()`、`execute()`。
2. 使用 `zod` 校验 tool input，并返回可读 validation error。
3. 为每次执行生成 `callId`、duration、startedAt、endedAt。
4. Verification: `npm test -- src/tools/registry.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M1-T05: Minimal Tools

**Objective:** 实现 `file_read`、`file_write`、`terminal_exec`，足够完成最小 coding loop。

**Files:**
- Create: `src/tools/file-read.ts`
- Create: `src/tools/file-write.ts`
- Create: `src/tools/terminal-exec.ts`
- Create: `src/tools/builtins.ts`
- Create: `src/tools/builtins.test.ts`

**Steps:**
1. `file_read` 支持相对路径、最大 2 MB、返回 `sha256` 和 `truncated`。
2. `file_write` 支持 create/replace，返回 pre/post hash 和 unified diff。
3. `terminal_exec` 使用 `child_process.spawn`，捕获 stdout/stderr、exitCode、timeout。
4. 允许 agent 用 `terminal_exec` 执行 `ls`/`dir` 来满足 `list files`。
5. Verification: `npm test -- src/tools/builtins.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

### M1-T06: Anthropic Provider Adapter

**Objective:** 接入 `@anthropic-ai/sdk`，把 streaming text 和 tool use 映射到内部格式。

**Files:**
- Create: `src/providers/anthropic.ts`
- Create: `src/providers/index.ts`
- Create: `src/providers/anthropic.test.ts`

**Steps:**
1. 从 `ANTHROPIC_API_KEY` 读取 key，缺失时 1 秒内输出设置提示并退出 code `4`。
2. 实现 `streamChat()`，支持 text delta 和 tool call 完成事件。
3. 将内部 tool schema 转为 Anthropic tool definition。
4. 使用 mock client 测试 text-only 和 tool-use 两条路径。
5. Verification: `npm test -- src/providers/anthropic.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

### M1-T07: Minimal Agent Loop

**Objective:** 实现 observe-act-reflect 最小循环，无 plan mode，无 dead code。

**Files:**
- Create: `src/core/agent-loop.ts`
- Create: `src/core/prompts.ts`
- Create: `src/core/index.ts`
- Create: `src/core/agent-loop.test.ts`
- Modify: `src/cli.ts`

**Steps:**
1. 将用户 prompt、可用 tool、工作目录注入 provider request。
2. 当 provider 返回 tool call 时，执行 tool，持久化 result，再继续下一轮。
3. 设置最大 5 轮，完成或 blocker 时输出明确 final response。
4. 处理 `Ctrl+C`：停止新 tool 调度并保存 interrupted session。
5. Verification: `npm test -- src/core/agent-loop.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

### M1-T08: First Runnable CLI E2E

**Objective:** 证明 M1 是真实可运行 CLI，而不是静态骨架。

**Files:**
- Create: `test/e2e/m1-list-files.test.ts`
- Create: `test/fixtures/mock-anthropic.ts`
- Modify: `package.json`
- Modify: `src/cli.ts`

**Steps:**
1. 增加 e2e 脚本，通过 mocked provider 返回 `terminal_exec` tool call。
2. 验证 `node dist/cli.js "list files"` 输出当前目录文件。
3. 验证 `.xycli/sessions/json/*.json` 包含 user message 和 tool call result。
4. Verification: `npm run build && npm test && node dist/cli.js "list files"`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

---

### M1-T09: 工具权限边界强制执行

**Objective:** Agent Loop 在调用工具前必须检查 `permissionLevel`，拦截越权调用。

**Files:**
- Modify: `src/core/agent-loop.ts`
- Create: `src/core/permission-guard.ts`
- Create: `src/core/permission-guard.test.ts`

**Steps:**
1. 从 CLI 配置读取 `permissionMode`（默认 `"auto-safe"`，仅允许 read-only + write-files + run-safe-commands）。
2. 实现 `PermissionGuard.check(tool.permissionLevel, config.permissionMode): boolean`。
3. 在 agent-loop 的 tool execution 前（第 164 行之前）插入 guard 检查。
4. 越权调用记录为 `denied` 状态，不执行工具，返回结构化错误信息。
5. 支持三个模式：`read-only`（只读工具）、`auto-safe`（默认，拒绝 full-access）、`full-access`（允许全部）。
6. Verification: `npm test -- src/core/permission-guard.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

---

## M2: More Tools And CLI UX

**Working CLI:** `xycli "search package.json"`、`xycli "show git status"`、`xycli --no-stream "list files"`

```
M2-T01 -> M2-T02 -> M2-T03
   |        |        |
   v        v        v
M2-T04 -> M2-T05 -> M2-T06
```

### M2-T01: Renderer With Streaming, Colors, And No-Color

**Objective:** 增加统一 renderer，支持 streaming、ANSI colors、`--no-stream` 和 `NO_COLOR`。

**Files:**
- Create: `src/cli/renderer.ts`
- Create: `src/cli/flags.ts`
- Create: `src/cli/renderer.test.ts`
- Modify: `src/cli.ts`

**Steps:**
1. 引入 `chalk` 和 `ora`。
2. 定义 renderer events：assistant text、tool start、tool output、tool done、status。
3. 支持 `--no-stream` buffered 输出。
4. Verification: `npm test -- src/cli/renderer.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M2-T02: Search Text Tool

**Objective:** 实现 `search_text`，优先使用 `rg`，不可用时用 Node fallback。

**Files:**
- Create: `src/tools/search-text.ts`
- Create: `src/tools/search-text.test.ts`
- Modify: `src/tools/builtins.ts`

**Steps:**
1. 输入支持 `query`、`path`、`glob`、`maxResults`、`caseSensitive`。
2. 默认最多 200 条，返回 path、line、column、text、truncated。
3. 遵守 `.gitignore` 和常见忽略目录。
4. Verification: `npm test -- src/tools/search-text.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M2-T03: Web Fetch Tool

**Objective:** 实现可禁用的 `web_fetch`，用于读取公开文档和错误页面。

**Files:**
- Create: `src/tools/web-fetch.ts`
- Create: `src/tools/web-fetch.test.ts`
- Modify: `src/tools/builtins.ts`

**Steps:**
1. 使用 Node `fetch` 获取 URL，默认最大 100 KB。
2. 记录 URL、status、contentType、fetchedAt、truncated。
3. 初版用简单 HTML text extraction，后续可替换 parser。
4. Verification: `npm test -- src/tools/web-fetch.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M2-T04: Git Status And Diff Tools

**Objective:** 增加 `git_status` 和 `git_diff`，让 agent 理解 workspace 状态。

**Files:**
- Create: `src/tools/git-status.ts`
- Create: `src/tools/git-diff.ts`
- Create: `src/tools/git.test.ts`
- Modify: `src/tools/builtins.ts`

**Steps:**
1. `git_status` 返回 isRepo、branch、head、dirtyFilesCount、stagedFilesCount。
2. `git_diff` 支持 working tree 和 staged diff。
3. 非 git repo 返回 structured warning，不视为 CLI 崩溃。
4. Verification: `npm test -- src/tools/git.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M2-T05: Progress And Idle Status

**Objective:** 长时间 provider/tool 操作时显示当前状态，减少无反馈等待。

**Files:**
- Modify: `src/core/agent-loop.ts`
- Modify: `src/tools/terminal-exec.ts`
- Modify: `src/cli/renderer.ts`
- Create: `src/cli/progress.test.ts`

**Steps:**
1. 超过 3 秒无 provider token 时显示 `thinking`。
2. tool 运行时显示 tool name、target、elapsed。
3. command 有 stdout/stderr 时 200 ms 内转发到 renderer。
4. Verification: `npm test -- src/cli/progress.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M2-T06: CLI UX E2E

**Objective:** 验证 M2 新 tools 和 UX 仍形成工作 CLI。

**Files:**
- Create: `test/e2e/m2-tools.test.ts`
- Modify: `test/fixtures/mock-anthropic.ts`

**Steps:**
1. 用 mocked provider 驱动 `search_text` 查询 repo 文件。
2. 用 mocked provider 驱动 `git_status`。
3. 验证 `--no-stream` 输出完整结果且 exit code 为 `0`。
4. Verification: `npm run build && npm test`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

---

## M3: Multi-Provider

**Working CLI:** `xycli --provider openai "list files"` and fallback from primary to secondary on mocked `429`

```
M3-T01 -> M3-T02 -> M3-T03 -> M3-T04 -> M3-T05 -> M3-T06
```

### M3-T01: Config Loader For Providers

**Objective:** 增加最小 config loader，先覆盖 provider/model/API key/env/temperature。

**Files:**
- Create: `src/core/config.ts`
- Create: `src/core/default-config.ts`
- Create: `src/core/config.test.ts`
- Modify: `src/cli.ts`

**Steps:**
1. 支持 CLI flags、environment variables、`.xycli/config.json` 三层。
2. 用 `zod` 校验 provider config。
3. 缺失 API key 时输出 provider-specific 设置提示。
4. Verification: `npm test -- src/core/config.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M3-T02: OpenAI Provider Adapter

**Objective:** 接入 `openai` SDK，并映射 tool calling 到内部格式。

**Files:**
- Create: `src/providers/openai.ts`
- Create: `src/providers/openai.test.ts`
- Modify: `src/providers/index.ts`

**Steps:**
1. 从 `OPENAI_API_KEY` 读取 key。
2. 实现 `streamChat()`，支持 text delta、function/tool call events、usage。
3. 将内部 tools 转为 OpenAI tool definitions。
4. Verification: `npm test -- src/providers/openai.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

### M3-T03: Provider Factory

**Objective:** 通过配置选择 Anthropic、OpenAI 或 generic OpenAI-compatible provider。

**Files:**
- Create: `src/providers/factory.ts`
- Create: `src/providers/generic-openai.ts`
- Create: `src/providers/factory.test.ts`
- Modify: `src/core/agent-loop.ts`

**Steps:**
1. 实现 `createProvider(config.providers.primary)`。
2. generic provider 支持 `baseUrl` 和 custom headers。
3. CLI 增加 `--provider`、`--model`、`--base-url`。
4. Verification: `npm test -- src/providers/factory.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M3-T04: Retry Policy

**Objective:** 对 transient provider failure 做指数退避重试。

**Files:**
- Create: `src/core/retry.ts`
- Create: `src/core/retry.test.ts`
- Modify: `src/core/agent-loop.ts`

**Steps:**
1. 对 `429`、`500`、`502`、`503`、network reset 重试最多 2 次。
2. 支持 jitter 和最大 delay。
3. 非 retryable provider error 直接返回 exit code `4`。
4. Verification: `npm test -- src/core/retry.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M3-T05: Circuit Breaker And Fallback

**Objective:** primary 连续两次 transient failure 后切换 fallback provider。

**Files:**
- Create: `src/core/circuit-breaker.ts`
- Create: `src/core/circuit-breaker.test.ts`
- Modify: `src/core/agent-loop.ts`
- Modify: `src/session/types.ts`

**Steps:**
1. 记录 provider failure count 和 cool-down。
2. session log 写入 fallback switch event。
3. fallback 缺失或 key 缺失时输出明确 blocker。
4. Verification: `npm test -- src/core/circuit-breaker.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M3-T06: Multi-Provider E2E

**Objective:** 验证 OpenAI path、Anthropic path、fallback path 都保持 CLI 可运行。

**Files:**
- Create: `test/e2e/m3-provider-fallback.test.ts`
- Modify: `test/fixtures/mock-anthropic.ts`
- Create: `test/fixtures/mock-openai.ts`

**Steps:**
1. mock OpenAI 返回 `terminal_exec`。
2. mock Anthropic 先返回两次 `429`，再切换 OpenAI fallback。
3. 检查 session JSON 中有 provider metadata 和 fallback event。
4. Verification: `npm run build && npm test`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

---

## M4: Persistence

**Working CLI:** `xycli session list`、`xycli session show <id>`、`xycli resume <id>`

```
M4-T01 -> M4-T02 -> M4-T03
   |        |        |
   v        v        v
M4-T04 -> M4-T05 -> M4-T06
```

### M4-T01: SQLite Store And Migrations

**Objective:** 引入 SQLite canonical store，JSON store 保留为 recovery fallback。

**Files:**
- Create: `src/session/sqlite-store.ts`
- Create: `src/session/migrations/001_initial.sql`
- Create: `src/session/sqlite-store.test.ts`
- Modify: `src/session/index.ts`
- Modify: `package.json`

**Steps:**
1. 安装 `better-sqlite3`。
2. 建立 `sessions`、`messages`、`tool_calls`、`token_usage` 基础表。
3. 默认路径 `.xycli/sessions/xycli.sqlite3`。
4. Verification: `npm test -- src/session/sqlite-store.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

### M4-T02: Session Repository Interface

**Objective:** 把 agent loop 从 JSON store 解耦，支持 SQLite 和 JSONL fallback。

**Files:**
- Create: `src/session/repository.ts`
- Create: `src/session/jsonl-recovery.ts`
- Modify: `src/core/agent-loop.ts`
- Modify: `src/session/types.ts`

**Steps:**
1. 定义 `SessionRepository`：create、appendMessage、recordToolCall、updateStatus、get。
2. SQLite 打开失败时写 JSONL recovery event 并给出 warning。
3. 确保每轮 loop 持久化为事务。
4. Verification: `npm run build && npm test`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M4-T03: Resume Command

**Objective:** 实现 `xycli resume <session-id>`，恢复历史上下文继续执行。

**Files:**
- Create: `src/cli/commands/resume.ts`
- Create: `src/core/resume-context.ts`
- Create: `src/core/resume-context.test.ts`
- Modify: `src/cli.ts`

**Steps:**
1. 读取 session messages 和 tool results。
2. 支持 optional resume prompt。
3. 恢复 provider/model/fallback metadata。
4. Verification: `npm test -- src/core/resume-context.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M4-T04: Session List And Show Commands

**Objective:** 提供可审计 session browser。

**Files:**
- Create: `src/cli/commands/session.ts`
- Create: `src/cli/format-table.ts`
- Create: `src/cli/commands/session.test.ts`
- Modify: `src/cli.ts`

**Steps:**
1. `xycli session list` 显示最近 50 个 id、title、status、created、updated、tokens。
2. `xycli session show <id>` 显示 messages 和 tool call 摘要。
3. 80 列终端下保持可读。
4. Verification: `npm test -- src/cli/commands/session.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M4-T05: Interrupt Persistence

**Objective:** `Ctrl+C` 后 2 秒内停止调度并保存 interrupted 状态。

**Files:**
- Modify: `src/cli.ts`
- Modify: `src/core/agent-loop.ts`
- Modify: `src/tools/terminal-exec.ts`
- Create: `test/e2e/m4-interrupt.test.ts`

**Steps:**
1. CLI 创建 root `AbortController`。
2. agent loop 收到 signal 后不再启动新 tool。
3. active tool 收到 signal 并记录 cancelled/interrupted。
4. Verification: `npm run build && npm test -- test/e2e/m4-interrupt.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M4-T06: Persistence E2E

**Objective:** 验证 SQLite session、resume、list/show 都可用。

**Files:**
- Create: `test/e2e/m4-session.test.ts`
- Modify: `test/fixtures/mock-anthropic.ts`

**Steps:**
1. 运行一次 `xycli "list files"`，提取 session id。
2. 执行 `xycli session list` 和 `xycli session show <id>`。
3. 执行 `xycli resume <id> "continue"` 并检查追加 message。
4. Verification: `npm run build && npm test`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 30 min

---

## M5: Plan Mode

**Working CLI:** `xycli --plan "modify README"` waits for approval before tools with side effects

```
M5-T01 -> M5-T02 -> M5-T03 -> M5-T04 -> M5-T05
```

### M5-T01: Plan Data Model

**Objective:** 定义 agent plan 状态并写入 session。

**Files:**
- Create: `src/core/plan.ts`
- Create: `src/core/plan.test.ts`
- Modify: `src/session/types.ts`
- Modify: `src/session/migrations/001_initial.sql`

**Steps:**
1. 定义 `AgentPlan`、`PlanStep`、`PlanStatus`。
2. session 保存 `plan_json`。
3. 提供 plan create/update/serialize helper。
4. Verification: `npm test -- src/core/plan.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M5-T02: Planning Prompt And Provider Flow

**Objective:** 在执行前先让 provider 生成计划，而不是立即执行 side effects。

**Files:**
- Modify: `src/core/prompts.ts`
- Modify: `src/core/agent-loop.ts`
- Create: `src/core/planning.test.ts`

**Steps:**
1. 增加 plan-only system instruction。
2. 将 provider response 解析为 user-readable steps。
3. plan 阶段不得执行 tools。
4. Verification: `npm test -- src/core/planning.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M5-T03: Approval Prompt

**Objective:** 用户批准 plan 后再进入 act loop。

**Files:**
- Create: `src/cli/approval-prompt.ts`
- Create: `src/cli/approval-prompt.test.ts`
- Modify: `src/cli.ts`
- Modify: `src/core/agent-loop.ts`

**Steps:**
1. 支持 approve、deny、edit instruction 三种输入。
2. deny 时 session 标记 completed/blocked，并输出 plan denied。
3. approve 后把计划作为 active plan 注入上下文。
4. Verification: `npm test -- src/cli/approval-prompt.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M5-T04: Plan Mode CLI Flags

**Objective:** 增加 `--plan`、`--no-plan`、`--auto-approve-plan`。

**Files:**
- Modify: `src/cli/flags.ts`
- Modify: `src/cli.ts`
- Modify: `src/core/config.ts`
- Create: `src/cli/plan-flags.test.ts`

**Steps:**
1. 默认保持 M1-M4 行为，`--plan` 显式启用。
2. `--auto-approve-plan` 只跳过计划确认，不跳过 tool permission。
3. config 中保留 plan mode 默认值。
4. Verification: `npm test -- src/cli/plan-flags.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 30 min

### M5-T05: Plan Mode E2E

**Objective:** 验证 plan mode 每次都先计划，再可控执行。

**Files:**
- Create: `test/e2e/m5-plan-mode.test.ts`
- Modify: `test/fixtures/mock-anthropic.ts`

**Steps:**
1. mock provider 首轮返回计划，批准后返回 tool call。
2. 验证 deny 不执行 `file_write`。
3. 验证 approve 后 CLI 正常完成。
4. Verification: `npm run build && npm test`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

---

## M6: Memory

**Working CLI:** `xycli memory list` and future runs include approved project/user memory

```
M6-T01 -> M6-T02 -> M6-T03 -> M6-T04 -> M6-T05
```

### M6-T01: Memory Store Schema

**Objective:** 增加跨 session memory 表，分用户偏好和项目上下文。

**Files:**
- Create: `src/memory/types.ts`
- Create: `src/memory/sqlite-memory-store.ts`
- Create: `src/session/migrations/002_memory.sql`
- Create: `src/memory/sqlite-memory-store.test.ts`

**Steps:**
1. 字段包含 id、scope、key、value、confidence、sourceSessionId、createdAt、updatedAt。
2. scope 支持 `user` 和 `project`。
3. memory store 与 session SQLite 共库。
4. Verification: `npm test -- src/memory/sqlite-memory-store.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M6-T02: Memory Extraction Suggestions

**Objective:** agent 完成后提出可保存 memory，不自动写入敏感内容。

**Files:**
- Create: `src/memory/extractor.ts`
- Create: `src/memory/extractor.test.ts`
- Modify: `src/core/agent-loop.ts`

**Steps:**
1. 从 final response 和 session summary 中提取候选偏好。
2. secret pattern 命中时拒绝候选。
3. 候选默认 pending，需要用户确认。
4. Verification: `npm test -- src/memory/extractor.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M6-T03: Memory Injection Into Context

**Objective:** 后续 session 自动注入已批准 memory。

**Files:**
- Create: `src/core/context-manager.ts`
- Create: `src/core/context-manager.test.ts`
- Modify: `src/core/agent-loop.ts`

**Steps:**
1. 在 system context 中包含 user preferences 和 project context。
2. 限制 memory token budget。
3. 标注 memory 来源，不覆盖显式用户输入。
4. Verification: `npm test -- src/core/context-manager.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M6-T04: Memory CLI Commands

**Objective:** 用户可查看、添加、删除 memory。

**Files:**
- Create: `src/cli/commands/memory.ts`
- Create: `src/cli/commands/memory.test.ts`
- Modify: `src/cli.ts`

**Steps:**
1. 实现 `xycli memory list`。
2. 实现 `xycli memory add --scope user --key <key> --value <value>`。
3. 实现 `xycli memory remove <id>`。
4. Verification: `npm test -- src/cli/commands/memory.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M6-T05: Memory E2E

**Objective:** 验证 memory 可跨 session 生效且 CLI 仍可执行。

**Files:**
- Create: `test/e2e/m6-memory.test.ts`
- Modify: `test/fixtures/mock-anthropic.ts`

**Steps:**
1. 添加一条 project memory。
2. 运行新 session，断言 provider request 包含 memory。
3. 删除 memory 后再次运行，断言不再注入。
4. Verification: `npm run build && npm test`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

---

## M7: Computer Use

**Working CLI:** `xycli "take screenshot"` and terminal tools support long-running interactive commands

```
M7-T01 -> M7-T02 -> M7-T03 -> M7-T04 -> M7-T05
```

### M7-T01: Extended Terminal Sessions

**Objective:** 支持长命令、idle prompt、发送 stdin、终止 command。

**Files:**
- Create: `src/tools/terminal-session.ts`
- Create: `src/tools/terminal-session.test.ts`
- Modify: `src/tools/terminal-exec.ts`
- Modify: `src/cli/renderer.ts`

**Steps:**
1. command 超过 5 秒无输出时发出 idle event。
2. CLI 支持 continue、send input、terminate。
3. result 包含 signal、last 200 lines summary、truncated。
4. Verification: `npm test -- src/tools/terminal-session.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

### M7-T02: Screenshot Tool

**Objective:** 增加本地 screenshot tool，供后续 browser/computer workflows 使用。

**Files:**
- Create: `src/tools/screenshot.ts`
- Create: `src/tools/screenshot.test.ts`
- Modify: `src/tools/builtins.ts`

**Steps:**
1. macOS 使用 `screencapture`，Linux/Windows 先返回 unsupported structured error。
2. 输出图片路径、format、width/height metadata。
3. 默认写入 `.xycli/artifacts/screenshots/`。
4. Verification: `npm test -- src/tools/screenshot.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M7-T03: Browser Automation Hook Interface

**Objective:** 定义 browser hooks，不绑定具体 browser 实现，保持 skeleton 可构建。

**Files:**
- Create: `src/computer/browser-hooks.ts`
- Create: `src/computer/types.ts`
- Create: `src/computer/browser-hooks.test.ts`

**Steps:**
1. 定义 `BrowserController` 接口：open、navigate、click、type、screenshot、close。
2. 提供 `NoopBrowserController`，未配置时返回 actionable error。
3. 给 tool registry 预留 browser tool registration。
4. Verification: `npm test -- src/computer/browser-hooks.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M7-T04: Browser Tool Stubs With Runtime Guards

**Objective:** 提供 `browser_open`、`browser_screenshot` tool，但没有 controller 时不执行副作用。

**Files:**
- Create: `src/tools/browser-open.ts`
- Create: `src/tools/browser-screenshot.ts`
- Create: `src/tools/browser.test.ts`
- Modify: `src/tools/builtins.ts`

**Steps:**
1. tool input/output schema 稳定。
2. 未启用 controller 时返回 `BROWSER_NOT_CONFIGURED`。
3. 已注入 mock controller 时可完成 e2e。
4. Verification: `npm test -- src/tools/browser.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M7-T05: Computer Use E2E

**Objective:** 验证 screenshot 和 extended terminal 不破坏既有 CLI。

**Files:**
- Create: `test/e2e/m7-computer-use.test.ts`
- Modify: `test/fixtures/mock-anthropic.ts`

**Steps:**
1. mock provider 调用 `screenshot`。
2. mock provider 调用一个会 idle 的 terminal command。
3. 验证结果写入 session，并输出 artifact path。
4. Verification: `npm run build && npm test`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

---

## M8: Extensibility

**Working CLI:** `xycli plugin list` and MCP stdio mock tools are discoverable and executable

```
M8-T01 -> M8-T02 -> M8-T03
   |        |        |
   v        v        v
M8-T04 -> M8-T05 -> M8-T06 -> M8-T07
```

### M8-T01: Npm Workspace Restructure

**Objective:** 将单包 skeleton 拆成 7-package monorepo，同时保持 `xycli` 可执行。

**Files:**
- Modify: `package.json`
- Create: `packages/cli/package.json`
- Create: `packages/core/package.json`
- Create: `packages/tools/package.json`
- Create: `packages/providers/package.json`
- Create: `packages/session/package.json`
- Create: `packages/mcp/package.json`
- Create: `packages/plugins/package.json`
- Move: `src/cli.ts` to `packages/cli/src/cli.ts`
- Move: `src/core/*` to `packages/core/src/`
- Move: `src/tools/*` to `packages/tools/src/`
- Move: `src/providers/*` to `packages/providers/src/`
- Move: `src/session/*` to `packages/session/src/`

**Steps:**
1. 根 `package.json` 配置 `workspaces: ["packages/*"]`。
2. CLI package 暴露 `bin.xycli`。
3. 修正 imports 为 `@xycli/core`、`@xycli/tools`、`@xycli/providers`、`@xycli/session`。
4. Verification: `npm run build && node packages/cli/dist/cli.js "list files"`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

### M8-T02: YAML Config And Allowlist/Denylist

**Objective:** 将配置扩展到 `.xycli/config.yaml` 和 `~/.xycli/config.yaml`，加入 allow/deny 规则。

**Files:**
- Create: `packages/core/src/config-schema.ts`
- Modify: `packages/core/src/config.ts`
- Create: `packages/core/src/config.test.ts`
- Modify: `packages/cli/src/cli.ts`

**Steps:**
1. 支持 CLI flag > env > project YAML > user YAML > defaults。
2. schema 包含 providers、tools、permissions、mcp、plugins。
3. validate error 输出字段路径和原因。
4. Verification: `npm test -- packages/core/src/config.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M8-T03: MCP Stdio Client

**Objective:** 实现最小 MCP stdio client，发现 tools 并桥接到 ToolRegistry。

**Files:**
- Create: `packages/mcp/src/client.ts`
- Create: `packages/mcp/src/tool-bridge.ts`
- Create: `packages/mcp/src/index.ts`
- Create: `packages/mcp/src/client.test.ts`

**Steps:**
1. 从 config 读取 command、args、env、cwd。
2. 启动 stdio server，执行 initialize 和 tools/list。
3. MCP tool 转为内部 `ITool`。
4. Verification: `npm test -- packages/mcp/src/client.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

### M8-T04: Plugin Manifest Validation

**Objective:** 读取和验证 plugin manifest，错误 plugin 不影响启动。

**Files:**
- Create: `packages/plugins/src/manifest.ts`
- Create: `packages/plugins/src/registry.ts`
- Create: `packages/plugins/src/index.ts`
- Create: `packages/plugins/src/manifest.test.ts`

**Steps:**
1. manifest 支持 name、version、entrypoint、permissions、commands、tools。
2. 使用 `zod` 返回 path-specific validation error。
3. registry 记录 loaded、failed、disabled 状态。
4. Verification: `npm test -- packages/plugins/src/manifest.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M8-T05: Plugin Tool Loading

**Objective:** 从 plugin entrypoint 加载 tools，并走同一 ToolRegistry。

**Files:**
- Create: `packages/plugins/src/loader.ts`
- Create: `packages/plugins/src/loader.test.ts`
- Create: `test/fixtures/plugins/echo-plugin/plugin.json`
- Create: `test/fixtures/plugins/echo-plugin/index.js`

**Steps:**
1. dynamic import plugin module。
2. 调用 `activate(context)`，允许 `registerTool()`。
3. 禁止 plugin 直接绕过 permission metadata。
4. Verification: `npm test -- packages/plugins/src/loader.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M8-T06: Plugin CLI Commands

**Objective:** 增加 `xycli plugin list`。

**Files:**
- Create: `packages/cli/src/commands/plugin.ts`
- Create: `packages/cli/src/commands/plugin.test.ts`
- Modify: `packages/cli/src/cli.ts`

**Steps:**
1. 显示 plugin name、version、path、status、error summary。
2. invalid plugin 显示 failed，不影响 exit code。
3. no plugin 时输出空表。
4. Verification: `npm test -- packages/cli/src/commands/plugin.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 30 min

### M8-T07: Extensibility E2E

**Objective:** 验证 workspace 拆包后 CLI、MCP、plugin 都工作。

**Files:**
- Create: `test/e2e/m8-extensibility.test.ts`
- Create: `test/fixtures/mcp/mock-server.ts`
- Modify: `test/fixtures/mock-anthropic.ts`

**Steps:**
1. 配置 mock MCP server 并发现 echo tool。
2. 配置 fixture plugin 并执行 plugin tool。
3. 验证 `xycli plugin list` 和 `xycli "use echo tool"`。
4. Verification: `npm run build && npm test`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

---

## M9: Safety

**Working CLI:** high-risk actions prompt, secrets are redacted, `xycli undo <session-id>` rolls back last file change

```
M9-T01 -> M9-T02 -> M9-T03
   |        |        |
   v        v        v
M9-T04 -> M9-T05 -> M9-T06 -> M9-T07
```

### M9-T01: Permission Engine

**Objective:** 中央化 permission evaluation，覆盖 built-in、MCP、plugin tools。

**Files:**
- Create: `packages/core/src/permissions/engine.ts`
- Create: `packages/core/src/permissions/types.ts`
- Create: `packages/core/src/permissions/engine.test.ts`
- Modify: `packages/tools/src/registry.ts`

**Steps:**
1. 支持 `read-only`、`write-files`、`run-safe-commands`、`network`、`full-access`。
2. 评估结果为 allow、deny、prompt。
3. deny rules 优先于 allow rules。
4. Verification: `npm test -- packages/core/src/permissions/engine.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

### M9-T02: Approval Gate For Side Effects

**Objective:** 写文件、网络、命令、高风险 action 执行前必须经过 approval gate。

**Files:**
- Create: `packages/cli/src/approval-gate.ts`
- Create: `packages/session/src/approvals.ts`
- Modify: `packages/core/src/agent-loop.ts`
- Modify: `packages/session/src/migrations/001_initial.sql`
- Create: `packages/cli/src/approval-gate.test.ts`

**Steps:**
1. prompt 显示 action、target、reason、risk、impact。
2. 用户 deny 时 tool call 记录为 denied。
3. 用户 approve 后执行并记录 approval id。
4. Verification: `npm test -- packages/cli/src/approval-gate.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M9-T03: Secret Redaction

**Objective:** 在 renderer、session、telemetry 之前统一 redaction。

**Files:**
- Create: `packages/core/src/security/redaction.ts`
- Create: `packages/core/src/security/redaction.test.ts`
- Modify: `packages/cli/src/renderer.ts`
- Modify: `packages/session/src/sqlite-store.ts`

**Steps:**
1. 默认 patterns 覆盖 API key、private key、token、password。
2. 支持用户自定义 patterns。
3. stdout/stderr、tool input/output、error details 写入前 redacted。
4. Verification: `npm test -- packages/core/src/security/redaction.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M9-T04: Risk Scanning For Terminal Commands

**Objective:** 默认阻止危险 command。

**Files:**
- Create: `packages/tools/src/terminal-risk.ts`
- Create: `packages/tools/src/terminal-risk.test.ts`
- Modify: `packages/tools/src/terminal-exec.ts`

**Steps:**
1. 检测 `rm -rf /`、`sudo`、credential dump、fork bomb 模式。
2. high-risk command 要求 explicit confirmation。
3. blocked command 返回 permission denied exit code `3`。
4. Verification: `npm test -- packages/tools/src/terminal-risk.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M9-T05: File Change Tracking

**Objective:** 每次写入记录 pre/post hash 和 unified diff。

**Files:**
- Create: `packages/session/src/file-changes.ts`
- Modify: `packages/tools/src/file-write.ts`
- Modify: `packages/session/src/migrations/001_initial.sql`
- Create: `packages/session/src/file-changes.test.ts`

**Steps:**
1. `file_write` 将 `FileWriteOutput` 传给 session file_changes 表。
2. 记录 operation create/modify/delete/patch。
3. dirty file 写入前提示已有未提交改动。
4. Verification: `npm test -- packages/session/src/file-changes.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M9-T06: Undo Command

**Objective:** 支持 `xycli undo <session-id>` 回滚最近一次未回滚文件变更。

**Files:**
- Create: `packages/cli/src/commands/undo.ts`
- Create: `packages/core/src/rollback.ts`
- Create: `packages/core/src/rollback.test.ts`
- Modify: `packages/cli/src/cli.ts`

**Steps:**
1. 根据 file_changes 找到最近未回滚变更。
2. 当前 hash 不等于 post hash 时停止并显示 conflict。
3. create 回滚为 delete，modify 回滚为 pre-image，delete 只回滚由 XYCLI 创建的文件。
4. Verification: `npm test -- packages/core/src/rollback.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

### M9-T07: Safety E2E

**Objective:** 验证安全能力覆盖实际 CLI 行为。

**Files:**
- Create: `test/e2e/m9-safety.test.ts`
- Modify: `test/fixtures/mock-anthropic.ts`

**Steps:**
1. mock provider 请求 `rm -rf /`，断言被拒绝。
2. mock provider 写入含 secret 内容，断言输出和 session 被 redacted。
3. mock provider 写文件后运行 `xycli undo <session-id>` 并验证文件恢复。
4. Verification: `npm run build && npm test`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

---

## M10: Polish

**Working CLI:** `xycli doctor` passes locally, package can be packed, CI validates build/test/e2e

```
M10-T01 -> M10-T02 -> M10-T03
    |         |         |
    v         v         v
M10-T04 -> M10-T05 -> M10-T06
```

### M10-T01: Diagnostics Doctor

**Objective:** 实现 `xycli doctor`，检查本地运行环境。

**Files:**
- Create: `packages/cli/src/commands/doctor.ts`
- Create: `packages/core/src/diagnostics.ts`
- Create: `packages/core/src/diagnostics.test.ts`
- Modify: `packages/cli/src/cli.ts`

**Steps:**
1. 检查 Node version、API key presence、git、shell、session store、config validity。
2. MCP/plugin failure 显示 warning，不阻断基础 doctor。
3. 支持 `--json` 输出 redacted report。
4. Verification: `npm test -- packages/core/src/diagnostics.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M10-T02: Opt-In Telemetry Skeleton

**Objective:** 建立 telemetry event 管道，默认关闭且不发送源码/prompt/stdout。

**Files:**
- Create: `packages/core/src/telemetry.ts`
- Create: `packages/core/src/telemetry.test.ts`
- Modify: `packages/core/src/config-schema.ts`

**Steps:**
1. 定义 event schema：command、duration、exitCode、tool names、error fingerprint。
2. 默认 `telemetry.enabled: false`。
3. payload 进入 sender 前强制 redaction。
4. Verification: `npm test -- packages/core/src/telemetry.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M10-T03: Npm Packaging

**Objective:** 打包为 npm package，提供稳定 `xycli` executable。

**Files:**
- Modify: `packages/cli/package.json`
- Modify: `package.json`
- Create: `.npmignore`
- Create: `LICENSE`
- Create: `README.md`

**Steps:**
1. 配置 package exports、bin、files。
2. 排除 tests、fixtures、local sessions、large artifacts。
3. 验证 unpacked package size 小于 20 MB。
4. Verification: `npm pack --dry-run`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M10-T04: CI Pipeline

**Objective:** GitHub Actions 覆盖 lint、typecheck、unit、e2e、package。

**Files:**
- Create: `.github/workflows/ci.yml`
- Modify: `package.json`

**Steps:**
1. matrix 覆盖 Node 18、20、22。
2. 运行 `npm ci`、`npm run build`、`npm test`、`npm pack --dry-run`。
3. 缓存 npm，避免缓存 `.xycli/sessions`。
4. Verification: `npm run build && npm test`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M10-T05: Command Surface And Exit Codes

**Objective:** 收敛 v1 command surface 和稳定 exit code。

**Files:**
- Modify: `packages/cli/src/cli.ts`
- Create: `packages/cli/src/exit-codes.ts`
- Create: `packages/cli/src/exit-codes.test.ts`
- Modify: `README.md`

**Steps:**
1. `xycli --help` 列出 run、resume、config、session、plugin、doctor、diff、undo、version。
2. 所有 `XycliError` 映射到 0-5 exit codes。
3. Windows/PowerShell 文档说明 local command 行为。
4. Verification: `npm test -- packages/cli/src/exit-codes.test.ts`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 45 min

### M10-T06: Final Release E2E

**Objective:** 发布前确认每个核心 workflow 可运行。

**Files:**
- Create: `test/e2e/m10-release.test.ts`
- Modify: `README.md`

**Steps:**
1. 测试 `xycli "list files"`、`session list`、`resume`、`doctor`、`plugin list`、`undo`。
2. 在 temp project 中执行 npm packed tarball。
3. 检查 no-color、80-column help、missing API key 提示。
4. Verification: `npm run build && npm test && npm pack --dry-run`。

**Verification:** Execute the `Verification:` command or check listed in Steps.

**Estimated:** 60 min

---

## Final Verification Checklist

- [ ] M1 最小 CLI 可运行：`node dist/cli.js "list files"` 或 `xycli "list files"` 成功。
- [ ] 每个 milestone 完成后都执行 `npm run build && npm test`。
- [ ] 每个 milestone 都至少有一个 e2e 测试证明 CLI 仍可工作。
- [ ] M1 没有未接线 dead code：provider、agent loop、tool registry、3 个 tools、JSON session 都在 CLI path 中被调用。
- [ ] Tool interface 支持 `name`、`description`、schema、permission level、timeout、structured result。
- [ ] Provider interface 支持 Anthropic、OpenAI、generic OpenAI-compatible 的统一 tool call representation。
- [ ] Session persistence 从 JSON 文件迁移到 SQLite 后，JSONL recovery fallback 仍可用。
- [ ] `Ctrl+C` 在 2 秒内停止新 tool 调度并保存 session 状态。
- [ ] `search_text` 优先使用 `rg`，没有 `rg` 时有 fallback。
- [ ] `web_fetch` 可通过 config 禁用，禁用时返回明确原因。
- [ ] `git_status` 和 `git_diff` 在非 git repo 中不导致 CLI crash。
- [ ] Plan mode 执行前展示计划，用户 deny 时不执行 side-effecting tools。
- [ ] Memory 只注入已批准内容，secret candidate 不保存。
- [ ] Browser/computer hooks 未配置时返回 actionable error，不产生不可控副作用。
- [ ] MCP 和 plugin tools 都通过同一个 ToolRegistry、permission engine、audit log。
- [ ] Allowlist/denylist 覆盖 command、path、domain、tool、MCP server、plugin。
- [ ] High-risk action 显示 action、target、reason、impact，并要求 explicit confirmation。
- [ ] Secret redaction 覆盖 renderer、session、diagnostics、telemetry。
- [ ] `xycli undo <session-id>` 在 hash conflict 时停止，不覆盖用户后续修改。
- [ ] `xycli doctor --json` 输出 redacted diagnostics，默认小于 1 MB。
- [ ] `xycli --help` 列出 run、resume、config、session、plugin、doctor、diff、undo、version。
- [ ] Exit codes 稳定：0 success、1 general、2 validation、3 permission、4 provider、5 tool。
- [ ] `npm pack --dry-run` 不包含 tests fixtures、local sessions、大型 artifacts，unpacked size 小于 20 MB。
- [ ] CI 覆盖 Node.js 18、20、22。
